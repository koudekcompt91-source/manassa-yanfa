import StudentCoursesCatalog from "@/components/student/StudentCoursesCatalog";

/** Same catalog as `/courses`; URLs kept for compatibility. Student shell labels use «الدورات». */
export default function PackagesPage() {
  return <StudentCoursesCatalog />;
}
